-- script that performs a specific task
-- script that performs a specific task
-- Create a TABLE named first_TABLE IF NOT EXISTS with one column id of type INT and a column name of type VARCHAR(256).
CREATE TABLE IF NOT EXISTS first_TABLE (
	id INT,
	name VARCHAR(256)
);
